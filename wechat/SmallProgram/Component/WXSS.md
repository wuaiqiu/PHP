# WXSS

```
样式导入
@import "common.wxss";

尺寸单位
rpx（responsive pixel）: 可以根据屏幕宽度进行自适应

选择器
.class      选择所有拥有 class="intro" 的组件
#id         选择拥有 id="firstname" 的组件
element     选择所有 element 组件
::after	   在组件后边插入内容
::before	   在组件前边插入内容
```