/*
 * String
 * 	charAt(index):返回在指定位置的字符
 * 	concat(str1..):连接字符串，返回新字符
 * 	replace(regexp/substr,replacement):替换substr为replacement
 * 	split(separator):将字符串转换为数组
 * 	substring(start,stop):裁剪字符序列
 * 	search(regexp)：返回匹配regexp的位置下标
 */