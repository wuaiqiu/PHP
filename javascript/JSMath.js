/*
 * Math
 *	 	Math.PI：圆周率
 * 		Math.abs(x):返回数的绝对值
 * 		Math.ceil(x):对数进行上舍入
 * 		Math.floor(x):对数进行下舍入
 * 		Math.round(x):把数四舍五入为最接近的整数
 * 		Math.max(x,y,...):返回最高值
 * 		Math.min(x,y,...):返回最低值
 * 		Math.pow(x,y):返回 x 的 y 次幂
 * 		Math.random():返回 0 ~ 1 之间的随机数。
 *
 */